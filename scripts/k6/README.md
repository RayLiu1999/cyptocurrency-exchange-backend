# 加密貨幣交易所 - K6 壓力測試與架構驗證腳本

本目錄收錄專為本交易所後端架構設計的 K6 測試腳本。從資深後端與分散式系統的觀點出發，這些腳本不僅僅是為了「製造假流量」，每一支腳本的存在都是為了**驗證特定的架構決策與系統隔離性**，例如：API Gateway 的限流反壓機制、Kafka 分區的平行處理能力、以及 In-Memory 引擎的極限吞吐量。

## 執行環境

```bash
# 確保後端服務已啟動
make test-up

# 安裝 k6（macOS）
brew install k6
```

---

## 腳本分類與效能指標對應 (Performance Metrics Mapping)

您在履歷或文件中看到的具體效能數據（如 40,000+ TPS、< 100ms 延遲），皆是由以下特定腳本在叢集環境 (ECS/K8s) 中運行得出的結果。本節明確區分每一支腳本的用途與其對應的關鍵效能指標。

### 1. 核心邏輯與交付防線 (CI/CD 必備)

這類腳本不追求極限 QPS，而是確保系統關鍵功能未被破壞。

| 腳本            | 架構驗證目的                                                                                                                                            | 對應目標 / 結論 |
| :-------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------- |
| `smoke-test.js` | **端到端業務邏輯驗證** (E2E Sanity Check)。模擬單一散戶完整生命週期 (註冊 -> 入金 -> 下單 -> 查單 -> 撤單)，確保微服務間的狀態轉換一致，無 regression。 | **CI Pass** (無 Error) |

### 2. 邊緣防禦與入口吞吐量 (Ingress & Gateway)

驗證第一道防線（API Gateway / Nginx / 限流器）的防護能力與多工寫入極限。

| 腳本                    | 架構驗證目的                                                                                                                                                               | 面試/履歷數據對應 |
| :---------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------- |
| `load-test.js`          | **常態滿載測試**。針對單張下單 API `/orders` 進行壓測。驗證系統在數千名虛擬散戶同時操作下，連線池與 API Gateway 的穩定度。                                                   | **並發能力 (Concurrency)**：驗證系統能穩定支撐數千 VU 同時在線。 |
| `market-maker-batch.js` | **造市商極限批次寫入 (Ingestion Batching)**。繞開 HTTP 次數與資料庫單筆 Tx 成本，展現系統最大 IO 寫入上限。 | **極致吞吐量 (Throughput)**：履歷上的 **"40,000+ TPS"** 寫入量就是由這支腳本測出。 |
| `spike-test.js`         | **突刺流量限流防護 (Rate Limit Resilience)**。模擬瞬間湧入超越 10 倍的流量（如惡意 DDoS 或大戶砸盤），驗證 Redis Sliding Window 的防護力。   | **高可用性防禦**：驗證系統能 **"優雅回傳 429"，達成 0% 的 500 Server Error 崩潰率**。 |

### 3. 微服務深度機制與撮合引擎 (Matching Engine & Kafka)

往系統深處打，找出下游微服務與訊息佇列的處理瓶頸。

| 腳本                               | 架構驗證目的                                                                                                                                                               | 面試/履歷數據對應                 |
| :--------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------- |
| `matching-engine-capacity-test.js` | **撮合引擎單點容量測試**。集中火力轟炸單一交易對，逼出 Kafka Partition 與 Matching Engine Goroutine 的物理極限。 | **引擎內部延遲**：驗證單點 In-Memory 撮合時間 **"< 2ms"**，找出 TPS 達到多少時 P99 會開始退化。          |
| `hot-vs-multi-symbol-test.js`      | **Kafka Partition 負載切分驗證**。對比「單一熱門幣種」與「分散多種幣種」下的 TPS 落差。藉此**證明**採用 Symbol 進行 Hash Routing 的設計確切有功效。                        | **水平擴展證明**：證明系統架構具備處理多交易對負載平衡的能力。 |

### 4. 資源隔離與端到端延遲 (Isolation & E2E Latency)

量測 Event-Sourcing 架構的致命點：非同步處理最終要花多少時間讓使用者看見。

| 腳本                   | 架構驗證目的                                                                                                                                                                    | 面試/履歷數據對應        |
| :--------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------- |
| `market-storm-test.js` | **模組資源隔離 (Resource Isolation)**。測試上萬 WebSocket 連線大量接收跨波時，下單服務是否會被拖垮。                 | **資源隔離證明**：驗證 Order 與 Market-Data 兩大微服務即便部署在同叢集，也不會互相搶佔資源。 |
| `e2e-latency-test.js`  | **全鏈路端到端延遲 (E2E Latency)**。將資料庫建立時間與收到 WS 推播的時間相減。 | **真實體感延遲**：履歷上的 **"P99 全鏈路延遲 < 100ms"** 客觀數據，就是由這支腳本計算得來。 |

---

## 執行範例與壓測除錯指南

### A. 為什麼我用壓測腳本跑出 0 筆成交 (Trades)？

如果使用 `load-test.js` 壓出了 10 萬筆訂單，卻發現資料庫裡 0 筆成交：

1. **價格過度發散**：因為腳本隨機生成的價格落差過大，無法交錯買賣單。
2. **撮合嚴重延遲 (Lagging)**：大門開太寬，但您背後的引擎還是單執行緒、讀取資料庫。這不是 Error，這正是我們需要開發「記憶體撮合引擎 (In-Memory Matching)」的最佳理由！

### B. 如何驗證分散式資料最終一致性 (Data Consistency Verification & Audit SQL)

壓測不只看 TPS 和 Latency，**更重要的是壓測後跑 Audit 驗證**。TPS 10 萬但資產不平，代表金融系統徹底失敗。
在執行高併發壓測後，我們提供了自動化的高標準核對程序，確保系統在混亂中依然遵循資產與狀態的最終一致性 (Invariants)。

請於壓測結束後執行以下自動化對帳腳本：

```bash
make test-audit
```

該腳本 (`scripts/audit/audit_test.go`) 會自動利用 SQL 驗證包含但不限於以下核心條件：

1. **Stuck Locked Funds (資金鎖定卡死)**
   - 找出資金被鎖 (`locked > 0`)，但沒有任何活躍訂單 (`status IN (1, 2)`) 的帳戶。
   - 代表資金被鎖定後，訂單被取消/完成卻沒有執行退款，引發用戶資產凍結的超嚴重問題。
2. **Stuck Orders (幽靈訂單)**
   - 確保所有活躍訂單在預期時間內得到狀態推進，若發現數分鐘前的訂單仍是 `NEW`，這暗示 Outbox 積壓或撮合引擎當機。
3. **Trade / Order 數量對帳**
   - 驗證每張訂單的 `filled_quantity` 等於其背後所有 `trades` 記錄中包含該訂單的 `quantity` 數量總和。
   - 保障結算邏輯沒有落單、沒有漏帳。
4. **Outbox 訊息發送一致性**
   - 確認所有 `outbox_messages` 都已經被成功處理，沒有過時滯留的 `Pending` 紀錄。
   - 驗證資料庫交易與 Kafka 訊息發佈達成最終一致，事件沒有遺失。
5. **負資產防禦 (Negative Balance Check)**
   - 驗證高併發與 Race Condition 環境下，所有帳戶的可用餘額與鎖定金額皆為大於等於 `0`。
   - 防禦極端情況導致的超扣與系統金庫擊穿。


### C. 重置污損的壓測資料庫

在每次極限壓測後，系統會遺留大量的未完成訂單與鎖定金額，請必定執行清理：

```bash
make db-fresh
```

### D. 執行精確路徑測試

```bash
# 驗證批次寫入效能 (測出 40k+ TPS 的關鍵)
k6 run scripts/k6/market-maker-batch.js

# 測實驗證端到端真實延遲 (P99 < 100ms 的關鍵)
k6 run scripts/k6/e2e-latency-test.js
```

